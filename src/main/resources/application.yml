server:
  port: 8080

spring:
  profiles:
    active: prod
  data:
    redis:
      host: redis
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus